eb init udagram-api --platform node.js --region us-east-1 &&
eb list &&
eb use udagram-api-env  &&
echo $(POSTGRES_HOST) &&
echo $POSTGRES_USERNAME "POSTGRES_USERNAME" &&
echo $POSTGRES_DB "POSTGRES_DB" && 
echo $POSTGRES_PASSWORD &&
echo $PORT &&
echo $AWS_DEFAULT_REGION &&
echo $AWS_PROFILE &&
echo $AWS_BUCKET &&
echo $URL &&
echo $JWT_SECRET &&
eb setenv POSTGRES_HOST=$POSTGRES_HOST POSTGRES_USERNAME=$POSTGRES_USERNAME POSTGRES_DB=$POSTGRES_DB POSTGRES_PASSWORD=$POSTGRES_PASSWORD PORT=$PORT AWS_REGION=$AWS_DEFAULT_REGION AWS_PROFILE=$AWS_PROFILE AWS_BUCKET=$AWS_BUCKET URL=$URL JWT_SECRET=$JWT_SECRET &&
eb deploy